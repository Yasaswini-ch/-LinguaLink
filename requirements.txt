# CPU-only PyTorch wheel index. Harmless as an extra index on any platform;
# specifically avoids pulling the much larger default CUDA-bundled wheel on
# Linux deployment targets (Streamlit Community Cloud, Docker, etc.).
--extra-index-url https://download.pytorch.org/whl/cpu

# Core ML
torch>=2.1
# Pinned exact, not >=: a newer transformers release (observed on Streamlit
# Cloud's environment) has a tokenizer-loading bug that incorrectly routes
# this (sentencepiece-based, non-tiktoken) XLM-R model through a tiktoken
# conversion path and fails even with tiktoken installed. 5.17.0 is verified
# working locally without needing tiktoken at all — pin to it rather than
# chase tiktoken's availability on whatever Python version Cloud is running.
transformers==5.17.0
tiktoken>=0.7            # kept as a safety net in case some environment still needs it
datasets>=2.19
sentence-transformers>=2.7
truecase>=0.0.14   # restores likely capitalization before NER (English only)
accelerate>=0.30
seqeval>=1.2.2          # NER span/F1 evaluation

# Knowledge base access
SPARQLWrapper>=2.0       # Wikidata SPARQL queries
requests>=2.31

# Data handling
pandas>=2.2
numpy>=1.26
pyyaml>=6.0

# Similarity search for candidate ranking
faiss-cpu>=1.8; platform_system != "Windows"
scikit-learn>=1.4        # fallback nearest-neighbor / TF-IDF baseline

# Demo — either the FastAPI backend (frontend is a separate React app in
# demo/frontend) or the self-contained Streamlit deployment (streamlit_app.py)
fastapi>=0.111
uvicorn[standard]>=0.29
streamlit>=1.60

# Dev / testing
pytest>=8.2
tqdm>=4.66
