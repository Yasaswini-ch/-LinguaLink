[project]
name = "mel"
version = "0.1.0"
description = "Multilingual Entity Linking & Disambiguation (EN/HI/ES/DE)"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
